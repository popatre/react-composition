export default function RandomPage({ word }) {
    return (
        <main>
            <h1>
                Im just another component that also uses the word of the day,
                which is... {word}
            </h1>
            <h2>
                I was created so that the word of the day needed to live in the
                app (for task 1)
            </h2>
        </main>
    );
}
